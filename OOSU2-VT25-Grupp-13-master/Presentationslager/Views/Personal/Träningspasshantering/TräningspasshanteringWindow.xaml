<Window x:Class="Presentationslager.Personal.Träningspasshantering.TräningspasshanteringWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Presentationslager.ViewModels"
        mc:Ignorable="d"
        Title="TräningspasshanteringWindow" Height="450" Width="800"
        Background="#6ab">

    <Window.DataContext>
        <vm:TräningspasshanteringViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Träningspasshantering" FontSize="25" FontWeight="Bold"/>
            <Button Content="Skapa Träningspass" Height="25" Width="200" Command="{Binding ÖppnaSkapaträningspassCommand}"/>
            <Button Content="Redigera Träningspass" Height="25" Width="200" Command="{Binding ÖppnaRedigeraträningspassCommand}"/>
            <Button Content="Ta bort Träningspass" Height="25" Width="200" Command="{Binding ÖppnaTabortträningspassCommand}"/>
            <Button Content="Hantera Deltagare" Height="25" Width="200" Command="{Binding ÖppnaHanteradeltagareCommand}"/>
            <Button Content="Tillbaka" Height="25" Width="200" Command="{Binding TillbakaCommand}"/>
        </StackPanel>
    </Grid>
</Window>