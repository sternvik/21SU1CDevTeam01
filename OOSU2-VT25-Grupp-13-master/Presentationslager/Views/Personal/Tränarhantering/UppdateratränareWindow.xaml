<Window x:Class="Presentationslager.Personal.Tränarhantering.UppdateratränareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Presentationslager.ViewModels"
        mc:Ignorable="d"
        Title="Uppdatera Tränare" Height="450" Width="800"
        Background="#6ab">

    <Window.DataContext>
        <vm:UppdateraTränareViewModel/>
    </Window.DataContext>

    <Grid>
        <TextBlock Text="Uppdatera Tränare" FontSize="25" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center"/>

        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Margin="50,60,0,0">
            <TextBlock Text="Tillgängliga tränare:" FontWeight="Bold"/>
            <DataGrid ItemsSource="{Binding Tränare}" SelectedItem="{Binding ValdTränare, Mode=TwoWay}" AutoGenerateColumns="True" Height="200" Width="400"/>
        </StackPanel>

        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Margin="450,60,0,0">
            <TextBlock Text="Tränaruppgifter:" FontWeight="Bold"/>
            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="Namn:"/>
                <TextBox Text="{Binding ValdTränare.Namn, Mode=TwoWay}" Height="25" Width="200"/>
            </StackPanel>

            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="Specialisering:"/>
                <ComboBox SelectedItem="{Binding ValdTränare.Specialisering, Mode=TwoWay}" ItemsSource="{Binding Specialiseringar}" Height="25" Width="200"/>
            </StackPanel>

            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="Lösenord:"/>
                <TextBox Text="{Binding ValdTränare.Lösenord, Mode=TwoWay}" Height="25" Width="200"/>
            </StackPanel>

            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="Bekräfta Lösenord:"/>
                <TextBox Text="{Binding LösenordConfirm, Mode=TwoWay}" Height="25" Width="200"/>
            </StackPanel>
        </StackPanel>

        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center" Orientation="Horizontal" Margin="0,0,0,30">
            <Button Content="Spara" Command="{Binding SparaCommand}" Height="30" Width="150" Margin="10,0"/>
            <Button Content="Tillbaka" Command="{Binding TillbakaCommand}" Height="30" Width="150" Margin="10,0"/>
        </StackPanel>
    </Grid>
</Window>
