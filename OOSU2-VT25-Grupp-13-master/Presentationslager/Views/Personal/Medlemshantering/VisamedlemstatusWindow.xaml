<Window x:Class="Presentationslager.Personal.Medlemshantering.VisamedlemstatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Presentationslager.ViewModels"
        mc:Ignorable="d"
        Title="Visa Medlemsstatus" Height="450" Width="800"
        Background="#6ab">

    <Window.DataContext>
        <vm:UppdateraMedlemViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Visa Medlemsstatus" FontSize="25" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Betalstatus:" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,50,0,0"/>

            <ComboBox ItemsSource="{Binding BetalstatusAlternativ}"
                      SelectedItem="{Binding ValdBetalstatus, Mode=TwoWay}"
                      Height="25" Width="200"
                      VerticalAlignment="Center" HorizontalAlignment="Center"
                     />

            <DataGrid ItemsSource="{Binding FiltreradeMedlemmar}" 
                      AutoGenerateColumns="True"
                      Width="700" Height="250"
                      VerticalAlignment="Center" HorizontalAlignment="Center"
                     />
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Button Content="Tillbaka" Height="25" Width="200"
                        Command="{Binding TillbakaCommand}"
                        VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
