<Window x:Class="Presentationslager.Personal.Medlemshantering.UppdateramedlemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Presentationslager.ViewModels"
        xmlns:Services="clr-namespace:Presentationslager.Services"
        xmlns:Converters="clr-namespace:Presentationslager.Converters"
        mc:Ignorable="d"
        Title="Uppdatera Medlem" Height="450" Width="800"
        Background="#6ab">

    <Window.Resources>
        <Converters:BoolToIndexConverter x:Key="BoolToIndexConverter" />
    </Window.Resources>

    <Window.DataContext>
        <vm:UppdateraMedlemViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Margin="25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Uppdatera medlem" FontSize="25" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center"/>

            <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Tillgängliga medlemmar:" FontWeight="Bold" />
                <DataGrid ItemsSource="{Binding Medlemmar}" SelectedItem="{Binding ValdMedlem, Mode=TwoWay}" 
                          AutoGenerateColumns="True" Height="200" Width="400" />
            </StackPanel>
            
            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Medlemsuppgifter:" FontWeight="Bold"/>
                <TextBlock Text="Namn:"/>
                <TextBox Text="{Binding ValdMedlem.Namn, Mode=TwoWay}" Height="25" Width="200"/>

                <StackPanel Margin="5">
                    <TextBlock Text="Lösenord:"/>
                    <PasswordBox Height="25" x:Name="LösenordBox" Services:LösenordService.Password="{Binding ValdMedlem.Lösenord, Mode=TwoWay}"/>
                </StackPanel>

                <TextBlock Text="Födelsedatum:"/>
                <DatePicker SelectedDate="{Binding ValdMedlem.Födelse, Mode=TwoWay}" Height="25" Width="200" />

                <TextBlock Text="Telefonnummer:" />
                <TextBox Text="{Binding ValdMedlem.Telefonnummer, Mode=TwoWay}" Height="25" Width="200" />

                <TextBlock Text="Epost:" />
                <TextBox Text="{Binding ValdMedlem.Epost, Mode=TwoWay}" Height="25" Width="200" />
                

                <TextBlock Text="Betalstatus:" />
                <ComboBox SelectedIndex="{Binding ValdMedlem.Betalstatus, Mode=TwoWay, Converter={StaticResource BoolToIndexConverter}}" Height="25" Width="200">
                    <ComboBoxItem Content="Betald" />
                    <ComboBoxItem Content="Obetald" />
                </ComboBox>
            </StackPanel>
        </Grid>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center">
            <Button Content="Spara" Command="{Binding SparaCommand}" Height="25" Width="100" Margin="0,10,10,0"/>
            <Button Content="Tillbaka" Command="{Binding TillbakaCommand}" Height="25" Width="100" Margin="0,10,10,0"/>
        </StackPanel>
    </Grid>
</Window>
