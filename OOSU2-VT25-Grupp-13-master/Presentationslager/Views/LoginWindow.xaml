<Window x:Class="Presentationslager.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Presentationslager.ViewModels"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800"
        Background="#6AB">

    <Window.DataContext>
        <vm:LoginWindowViewModel/>
    </Window.DataContext>

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,5,0,5">
        <TextBlock Text="Logga in" FontSize="24" FontWeight="Bold" TextAlignment="Center"/>

        <TextBlock Text="Användarnamn:"/>
        <TextBox Text="{Binding Användarnamn, UpdateSourceTrigger=PropertyChanged}" Width="200" Height="25"/>

        <TextBlock Text="Lösenord:"/>
        <PasswordBox Width="200" Height="25" PasswordChanged="PasswordBox_PasswordChanged"/>

        <StackPanel Margin="0,10,0,0">
            <Button Content="Logga in" Width="200" Height="25" Command="{Binding LoggaInCommand}"/>
            <Button Content="Tillbaka" Width="200" Height="25" Command="{Binding TillbakaCommand}"/>
        </StackPanel>
    </StackPanel>
</Window>
