<Window x:Class="Presentationslager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentationslager"
        xmlns:vm="clr-namespace:Presentationslager.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Background="#6AB">

    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Definiera en stil för alla knappar i fönstret och tilldela samma mellanrum (margin) för dem -->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="SportClub" FontSize="24" FontWeight="Bold" TextAlignment="Center"/>

            <Button Content="Logga in" Height="25" Width="150" Command = "{Binding LoggaInCommand}"/>
            <Button Content="Registrera" Height="25" Width="150" Command = "{Binding RegistreraCommand}"/>
            <Button Content="Avsluta" Height="25" Width="150" Command = "{Binding AvslutaCommand}"/>
        </StackPanel>
    </Grid>
</Window>
