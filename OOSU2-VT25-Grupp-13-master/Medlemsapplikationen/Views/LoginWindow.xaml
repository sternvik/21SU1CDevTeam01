<Window x:Class="Medlemsapplikationen.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Medlemsapplikationen.Views"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:Medlemsapplikationen.ViewModels"
        Title="LoginWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:LoginWindowViewModel />
    </Window.DataContext>

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="SlateGray"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="BorderBrush" Value="DarkSlateGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="DarkSlateGray"/>
                    <Setter Property="Foreground" Value="LightGray"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Image Source="/Images/tennis-7932067_1280.jpg"
           Stretch="Fill"/>
        <Border Background="SlateGray" CornerRadius="15" Padding="20"
    Width="450" Height="300"
    HorizontalAlignment="Center" VerticalAlignment="Center"
    >
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,5,0,5">
                <TextBlock Text="Logga in" FontSize="20" Foreground="White" FontWeight="Bold"
               HorizontalAlignment="Center"/>

                <TextBlock Text="Användarnamn:" Foreground="White" FontWeight="Bold"/>
                <TextBox Text="{Binding Användarnamn, UpdateSourceTrigger=PropertyChanged}" Width="200" Height="25"/>

                <TextBlock Text="Lösenord:" Foreground="White" FontWeight="Bold"/>
                <PasswordBox Width="200" Height="25" PasswordChanged="PasswordBox_PasswordChanged"/>

                <StackPanel>
                    <Button Content="Logga in" Width="200" Command="{Binding LoggaInCommand}" Margin="0,5,0,5"  Style="{StaticResource MenuButtonStyle}"/>
                    <Button Content="Tillbaka" Width="200" Command="{Binding TillbakaCommand}"  Style="{StaticResource MenuButtonStyle}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
